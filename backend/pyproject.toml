[project]
name = "backend"
version = "0.1.0"
description = "Backend for the Study Buddy RAG LLMOps Project"
requires-python = ">=3.13"

dependencies = [
    "lancedb",
    "pydantic-ai", 
    "fastapi", 
    "uvicorn", 
    "mlflow"
]