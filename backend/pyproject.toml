[project]
name = "backend"
version = "0.1.0"
description = "Backend for Studdy Buddy RAG llmops project"
requires-python = ">=3.13"

dependencies = [
    "lancedb",
    "pydantic-ai", 
    "fastapi", 
    "uvicorn", 
    "mlflow"
]