[project]
name = "fullstack-llmops-project"
version = "0.1.0"
description = "Fullstack project with RAG"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
    "mlflow>=3.11.1",
]
